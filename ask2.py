print("ASKHSH 2")
class Rectangle:
    def __init__(self, length, width):
        self.length = length
        self.width = width
    def CalcArea(self):
        self.emvadon =  self.length*self.width
        
sxima = Rectangle(5,2)
sxima.CalcArea()
print(sxima.emvadon)